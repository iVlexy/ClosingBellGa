apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: livelyfencing
spec:
  selector:
    app: ollama
  ports:
    - port: 11434
      targetPort: 11434
  # ClusterIP only — Ollama is internal, never exposed externally
  type: ClusterIP
