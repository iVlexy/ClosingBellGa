apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-secret
  namespace: livelyfencing
type: Opaque
stringData:
  # Paste your tunnel token from: cloudflared tunnel token <tunnel-name>
  TUNNEL_TOKEN: REPLACE_WITH_CLOUDFLARE_TUNNEL_TOKEN
